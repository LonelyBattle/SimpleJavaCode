<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zs.test.WifeMapper">
    <select id="selectOne" parameterType="Integer" resultType="com.zs.entity.Wife">
        select * from `wife` where id = #{id}
    </select>
</mapper>